import matplotlib.pyplot as plt

def generaHistograma(filename):
	fig = Figure()
	N = {1 : 10, 2 : 20, 3 : 40}
	plt.bar(N.keys(), N.values(), align='center')
	plt.savefig(filename)
	canvas = FigureCanvas(fig)
	return canvas

